[project]
name = "research-workspace"
version = "0.1.0"
description = "Research workspace for LLM epistemic belief experiments"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
